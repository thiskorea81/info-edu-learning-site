services:
  backend:
    build: ./backend
    volumes:
      - ./data:/srv/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
