당신은 /home/student/Documents/study/std01 사이트(정보교육학습사이트)에 매일 코딩테스트 문제를 추가하는 무인 자동화 작업입니다. 사람이 지켜보지 않으므로 스스로 검증하고, 문제가 있으면 커밋하지 말고 중단하세요.

작업 디렉터리: /home/student/Documents/study/std01

이 사이트의 코딩테스트 문제는 기본문제/일일문제/교과서문제(성취기준별)로 1차 분류되고, 이와 별개로(중복 가능) AtCoder 실제 대회처럼 A~G형으로도 분류된다. 문제 JSON에 `"유형": "A"`~`"G"` 필드를 넣으면 그 문제는 소속 1차 카테고리(예: 일일문제)에 그대로 남으면서 동시에 A~G형 카테고리에도 나타난다(백엔드 `backend/app/routers/problems.py`의 `_category_of`는 기본/일일/교과서만 판단하고, `_type_of`가 유형을 별도로 판단해 두 분류가 서로 겹칠 수 있게 되어 있다).

이 자동화 작업은 **매일 A~G형 문제를 하나씩, 총 7개를 만들어 일일문제로 추가**하는 역할을 한다. 즉 오늘 하루치 결과물은 "오늘의 일일문제 7개(A형 1개, B형 1개, …, G형 1개)"이다.

다음을 순서대로 수행하세요:

1. 오늘 날짜(로컬 기준, YYYY-MM-DD)를 `date +%F` 로 확인하세요. 오늘 날짜로 만들 파일 경로는 `data/problems/daily_<YYYY-MM-DD>.json` 입니다. 이 파일이 이미 있다면 내용을 확인하고 문제가 없으면 그대로 두고 종료하세요 — 중복 생성하지 마세요.

2. `data/problems/*.json`의 기존 문제들을 모두 읽어서 이미 사용된 id와 제목/주제를 파악하세요. 오늘 만들 7개 문제는 기존 문제(특히 같은 유형의 문제들)와 주제가 겹치지 않게 하세요.

3. A, B, C, D, E, F, G 7개 유형 각각에 대해 AtCoder Beginner Contest 스타일의 파이썬 자동 채점 문제를 1개씩, 총 7개 만드세요. 유형별 난이도 감(참고용, 엄격한 규칙 아님. `data/problems/type_a_seed.json` ~ `type_g_seed.json`의 기존 예시들을 참고하면 난이도 감을 잡기 좋습니다):
   - A, B: 쉬움 — 산술/문자열/조건문/기본 반복문 수준
   - C, D: 보통 — 그리디, 기본 동적 계획법, 정렬 활용 등
   - E, F, G: 어려움 — BFS/DFS, 유니온-파인드, 다익스트라 등 그래프·고급 자료구조

   정답이 하나로만 명확히 결정되도록 문제/제약조건을 정확하게 서술하세요(입력 범위, 시간 제한, "가장 큰" 등 표현에는 기준 명시).

4. 각 문제는 `data/problems/type_a_seed.json` ~ `data/problems/type_g_seed.json`과 같은 스키마를 정확히 따르세요. 다음 필드를 모두 포함해야 합니다:
   - id: "p_daily_<오늘날짜 YYYYMMDD>_<유형 소문자>" (예: A형이면 p_daily_20260714_a, 기존 id와 겹치지 않게)
   - 유형: "A"~"G" 중 해당 값
   - title, difficulty(쉬움/보통/어려움), time_limit_ms(1500~2000 사이), statement, constraints, input_format, output_format
   - samples: 최소 1개 (input/output/explanation 포함)
   - tests: 최소 6개 (일반적인 경우와 경계값 포함)
   - 힌트: 정답을 바로 드러내지 않는 1~2문장 힌트
   - 해설: 어떤 아이디어/접근으로 풀어야 하는지에 대한 설명
   - 정답_코드: 한글 주석이 포함된, 여러 줄로 읽기 좋게 정리한 파이썬 정답 코드
   - 시간복잡도: 예) "O(N) — 리스트를 한 번만 순회한다." 형식으로 간단한 근거와 함께 표기

5. 매우 중요 — 절대 손으로 정답을 계산하지 마세요: 7개 문제 각각의 정답_코드를 실제로 실행해서 그 출력을 samples와 tests의 expected output으로 사용하세요. 저장하기 전에 정답_코드를 각 테스트 입력에 대해 다시 한번 실행해서 저장된 output과 정확히 일치하는지 문제마다 재검증하세요.

6. 검증까지 끝난 7개 문제를 하나의 배열로 감싸 `data/problems/daily_<YYYY-MM-DD>.json` 파일 하나에 저장하세요(유형별로 파일을 나누지 않습니다).

7. 저장한 JSON 파일이 유효한 JSON인지 확인하세요:
   `python3 -c "import json; d=json.load(open('data/problems/daily_<날짜>.json', encoding='utf-8')); assert len(d)==7"`

8. `scripts/commit_and_deploy.sh "일일 문제 자동 추가(A~G형 7문제): <날짜>" data/problems/daily_<날짜>.json` 를 실행하세요. 이 스크립트가 커밋 → `study-backend.service` 재시작(배포) → `/api/standards` 응답 확인까지 한 번에 처리합니다. push는 하지 않습니다(별도의 `study-daily-push.timer`가 매일 09:00에 자동으로 push합니다).

9. 다음 명령으로 '일일 문제' 카테고리 수와 A~G 각 유형 카테고리 수가 정상적으로 늘어났는지 확인하세요:
   `curl -s "http://localhost:8000/api/problems/categories" | python3 -c "import json,sys; d=json.load(sys.stdin); print([c for c in d if c['key']=='daily' or c['key'].startswith('type_')])"`

10. 이 중 어느 단계에서든 실패하거나(JSON 파싱 오류, 검증 불일치, commit_and_deploy.sh 실패 등) 애매하면 즉시 중단하고 무엇이 잘못됐는지 한두 문장으로 요약해 출력하세요. 검증되지 않은 데이터는 절대 저장/커밋하지 마세요. 7개 중 일부만 검증에 실패했다면 전체를 저장하지 말고 중단하세요(부분 저장 금지).
